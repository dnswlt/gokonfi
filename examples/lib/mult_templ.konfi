// This file declares multiple templates to be used in other files.

// A module-local let binding.
let h: 101

// An exported (public) template.
pub template job_tmpl(params) {
    let p: {
        f: 3
    } @ params
    a: p.f
    b: p.f * p.f
}

// An exported function.
pub func f(x) { x * x }

// The module body.
{ 
    x: h * h
    note: "this is the module's body expression"
}
